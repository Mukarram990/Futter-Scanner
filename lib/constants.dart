// ignore_for_file: constant_identifier_names
import 'package:flutter/material.dart';

const Color Kmaincolor = Colors.red;

Border klistborder = const Border(
  bottom: BorderSide(color: Color.fromARGB(129, 212, 30, 17), width: 1),
);

const TextStyle ktextstyle = TextStyle(
  fontSize: 17,
);

const ListTileThemeData klistdata = ListTileThemeData(
  iconColor: Kmaincolor,
  textColor: Kmaincolor,
  selectedColor: Colors.white,
  selectedTileColor: Kmaincolor,
);
